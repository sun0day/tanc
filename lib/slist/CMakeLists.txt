SET(LIB_NAME slist)
SET(LIB_HEADER slist.h)
SET(LIB_SRC slist.c)
SET(LIB_TEST_BIN slist_test)

ADD_LIBRARY(${LIB_NAME} SHARED ${LIB_SRC})
ADD_LIBRARY(${LIB_NAME}_static STATIC ${LIB_SRC})

SET_LIB_TARGET()
INSTALL_HEADER("${LIB_NAME}")
INSTALL_LIB("${LIB_NAME}")

UT_RUN()
